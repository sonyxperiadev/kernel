# SPDX-License-Identifier: GPL-2.0

dtbo-$(CONFIG_ARCH_PARROT) += \
	parrot-sde.dtbo \
	parrot-sde-display-qrd-overlay.dtbo

always-y    := $(dtb-y) $(dtbo-y)
subdir-y    := $(dts-dirs)
clean-files := *.dtb *.dtbo
