# SPDX-License-Identifier: GPL-2.0

dtbo-$(CONFIG_ARCH_KALAMA) += \
	kalama-ese-qrd.dtbo \
	kalama-v2-ese-qrd.dtbo

always-y    := $(dtb-y) $(dtbo-y)
subdir-y    := $(dts-dirs)
clean-files := *.dtb *.dtbo
