# SPDX-License-Identifier: GPL-2.0

# SoMC Columbia
dtbo-$(CONFIG_ARCH_SONY_COLUMBIA) += \
	parrot-columbia-pdx246_generic-overlay.dtbo

always-y    := $(dtb-y) $(dtbo-y)
subdir-y    := $(dts-dirs)
clean-files := *.dtb *.dtbo
